<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Labels" parent="Theme.MaterialComponents.DayNight">
    </style>

    <style name="Theme.Labels.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Labels.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Labels.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>